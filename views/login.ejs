<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Login</title>
    <!-- Use Bootstrap CSS from node_modules -->
    <link rel="stylesheet" href="/bootstrap/css/bootstrap.min.css">
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@400;600&display=swap" rel="stylesheet">

    <style>
        body {
            font-family: 'Poppins', sans-serif; /* Custom font */
        }

        .bg-light-custom {
            background-color: #f8f9fa; /* Light Grey background */
        }

        .card-custom {
            background-color: #ffffff; /* White background for card */
            border-radius: 8px;
            box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1); /* Light shadow */
        }

        .card-header-custom {
            background-color: #ffffff; /* White background for header */
            border-bottom: 1px solid #ddd; /* Light border for header */
        }

        .btn-custom {
            background-color: #28a745; /* Green button color */
            border-color: #28a745;
            color: white;
        }

        .btn-custom:hover {
            background-color: #218838;
            border-color: #1e7e34;
        }

        .footer-link {
            color: #28a745;
            font-weight: 600;
            text-decoration: none;
        }

        .footer-link:hover {
            text-decoration: underline;
        }

        .form-control-custom {
            border-radius: 4px;
            box-shadow: none; /* Remove box shadow for inputs */
            border: 1px solid #ced4da; /* Light border for inputs */
        }

        .form-label-custom {
            font-weight: 500;
            color: #495057;
        }

        .text-center-custom {
            text-align: center;
            color: #495057;
        }

        .signup-link {
            color: #28a745;
            font-weight: 600;
            text-decoration: none;
        }

        .signup-link:hover {
            text-decoration: underline;
        }
    </style>
</head>
<body class="bg-light-custom d-flex flex-column min-vh-100">
    <main class="flex-grow-1 d-flex justify-content-center align-items-center">
        <div class="container">
            <div class="row">
                <div class="col-9 col-lg-4 mx-auto">
                    <div class="card card-custom">
                        <div class="card-header card-header-custom text-center my-3 border-none">
                            <h1 class="fs-3">Market Monitoring System</h1>
                        </div>
                        <div class="card-body col-10 col-lg-9 mx-auto">
                            <form action="./login" method="POST">
                                <div class="mb-3">
                                    <input type="text" id="username" name="username" class="form-control form-control-custom" placeholder="Username" required>
                                </div>
                                <div class="mb-3">
                                    <input type="password" id="password" name="password" class="form-control form-control-custom" placeholder="Password" required>
                                </div>
                                <button type="submit" class="btn btn-custom w-100">Login</button>
                            </form>
                            <% if (typeof error !== 'undefined') { %>
                                <p class="text-danger text-center mt-3"><%= error %></p>
                            <% } %>
                        </div>
                        <div class="text-center bg-light my-4">
                            <p class="mb-2">Don't have an account? <a href="/user/signup" class="signup-link">Sign Up</a></p>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </main>
    <%- include('partials/footer') %>
</body>
</html>
